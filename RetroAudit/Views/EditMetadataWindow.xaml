<Window x:Class="RetroAudit.Views.EditMetadataWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Metadata" Height="480" Width="440" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Background="{StaticResource Brush.Background.Base}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" Margin="16" VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,0,0,10">
                    <TextBlock Text="Başlık" Foreground="{StaticResource Brush.Text.Secondary}" FontSize="11" Margin="0,0,0,2" />
                    <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,0,10">
                    <TextBlock Text="Tür" Foreground="{StaticResource Brush.Text.Secondary}" FontSize="11" Margin="0,0,0,2" />
                    <TextBox Text="{Binding Genre, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,0,0,10">
                    <TextBlock Text="Publisher" Foreground="{StaticResource Brush.Text.Secondary}" FontSize="11" Margin="0,0,0,2" />
                    <TextBox Text="{Binding Publisher, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.Column="0" Margin="0,0,0,10">
                    <TextBlock Text="Developer" Foreground="{StaticResource Brush.Text.Secondary}" FontSize="11" Margin="0,0,0,2" />
                    <TextBox Text="{Binding Developer, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
                <StackPanel Grid.Row="2" Grid.Column="2" Margin="0,0,0,10">
                    <TextBlock Text="Video URL" Foreground="{StaticResource Brush.Text.Secondary}" FontSize="11" Margin="0,0,0,2" />
                    <TextBox Text="{Binding VideoUrl, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

                <StackPanel Grid.Row="3" Grid.Column="0" Margin="0,0,0,10">
                    <TextBlock Text="Yıl" Foreground="{StaticResource Brush.Text.Secondary}" FontSize="11" Margin="0,0,0,2" />
                    <TextBox Text="{Binding ReleaseYearText, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
                <StackPanel Grid.Row="3" Grid.Column="2" Margin="0,0,0,10">
                    <TextBlock Text="Region" Foreground="{StaticResource Brush.Text.Secondary}" FontSize="11" Margin="0,0,0,2" />
                    <ComboBox ItemsSource="{Binding RegionOptions}" Text="{Binding Region, UpdateSourceTrigger=PropertyChanged}"
                              IsEditable="True" IsTextSearchEnabled="False" />
                </StackPanel>

                <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,0,0,10">
                    <TextBlock Text="Açıklama" Foreground="{StaticResource Brush.Text.Secondary}" FontSize="11" Margin="0,0,0,2" />
                    <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                             TextWrapping="Wrap" AcceptsReturn="True" Height="72" VerticalScrollBarVisibility="Auto" />
                </StackPanel>
            </Grid>
        </ScrollViewer>

        <Border Grid.Row="1" Background="{StaticResource Brush.Background.Panel}"
                BorderBrush="{StaticResource Brush.Border}" BorderThickness="0,1,0,0" Padding="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Command="{Binding CancelCommand}" Width="90" Margin="0,0,8,0" />
                <Button Content="Save" Command="{Binding SaveCommand}" Width="90" Style="{StaticResource LaunchButton}" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
